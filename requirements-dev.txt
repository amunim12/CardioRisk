# Development + optional runtime dependencies.
# Install with:  pip install -r requirements-dev.txt
-r requirements.txt

# Quality gates
ruff>=0.5
mypy>=1.10
pytest>=8.2
pytest-cov>=5.0
httpx>=0.27
pre-commit>=3.7
types-PyYAML>=6.0
pandas-stubs>=2.2

# Optional runtime extras (guarded in code; may lack wheels on very new
# Python versions — failures here do NOT break the core pipeline).
mlflow>=2.14
shap>=0.45
streamlit>=1.36
